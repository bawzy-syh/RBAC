<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.syh.RBAC.mapper.AccountMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="org.syh.RBAC.model.Account">
        <id column="uid" property="uid" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="email" property="email" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <select id="getUserRole" resultType="java.lang.String">
        SELECT role_name
        FROM `role`,user_role_relation
        WHERE `role`.roleID = user_role_relation.roleID AND user_role_relation.userID=#{uid}
    </select>
</mapper>
